async def run_orchestrator(message: str, history: list) -> dict:
    # Placeholder — we'll replace this in Phase 4
    return {
        "reply": f"Orchestrator not built yet. You said: {message}",
        "agent_used": "none"
    }